version: '3.8'

services:
  repair-app:
    build: .
    container_name: repair-manager-app
    ports:
      - "6000:6000"
    volumes:
      - ./instance:/app/instance
    environment:
      - TZ=Africa/Maputo
    restart: always
